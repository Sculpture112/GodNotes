#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    vector<bool> distanceLimitedPathsExist(int n, vector<vector<int>> &edgeList, vector<vector<int>> &queries)
    {
        int n = edgeList.size();
        int m = queries.size();

        sort(edgeList.begin(),edgeList.end(),[](auto & a,))
    }
};