#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    int minTransfers(vector<vector<int>> &transactions)
    {

    }
    vector<int> dept(vector<vector<int>>& transaction){
        for (int i = 0;i<n)
    }
};