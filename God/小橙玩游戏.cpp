#include <bits/stdc++.h>
using namespace std;

using ll = long long;

const int INF = 0x3f3f3f3f;
const ll LINF = 4e18;

#define all(x) (x).begin(), (x).end()
bool F = true;
bool f(int x)
{
    if (x <= 0)
    {
        return false;

    }
    x -= F?
}
void solve()
{
    int n;
    cin >> n;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T = 1;
    // cin >> T;
    cin >> T;
    while (T--)
        solve();

    return 0;
}