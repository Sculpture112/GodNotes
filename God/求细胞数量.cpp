#include <bits/stdc++.h>
using namespace std;
int n, m;

void solve()
{
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> n >> m;
    vector<vector<int>> grid(n, vector<int>(m));
    for (int i =)

        solve();

    return 0;
}