#include <bits/stdc++.h>
using namespace std;
int m, n;
int dfs(int x)
{
    if (x == n)
    {
        return 1;
    }

    if()
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> m >> n;

    return 0;
}