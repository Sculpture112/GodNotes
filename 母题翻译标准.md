# 母题翻译标准

本文档记录把 `src` 目录下 Java 代码翻译成 `母题` 目录 Markdown 文件时的注意点。

## 总原则

- “母题”文件是 Java 代码先翻译成 C++，再套入母题模板后的结果。
- Java 源文件统一从根目录下的 `src` 文件夹读取。
- 每次只创建新文件，不覆盖、修改已经处理好的旧母题文件。
- 翻译目标是竞赛可读、可提交的 C++，不要机械保留 Java 写法。
- C++ 默认使用 `#include <bits/stdc++.h>`。
- 代码风格尽量使用 ACM 竞赛风格。
- 不要使用 `static`。

## 文件命名

- 新文件放在根目录下的 `母题` 文件夹中。
- 文件名沿用已有母题文件的风格：
  - 课时编号使用三位数，如 `080`
  - 保留 `CodeXX`
  - 后面接题目中文名
  - 扩展名为 `.md`
- 示例：`080 Code03 划分为k个相等的子集.md`
- 如果目标文件名已经存在，跳过该文件，不要覆盖。

## Markdown 模板

- 使用根目录下的 `template/templatec++.md` 作为外壳。
- 保留母题文件常见结构：
  - 标题
  - 原题链接
  - 涉及知识点
  - 核心套路
  - 破题切入点
  - C++ 代码块
- `原题链接` 从 Java 文件里的 `测试链接` 提取并填入。
- `涉及知识点` 不要主动填写，保留空占位或模板原样。
- 不要为了补全文档而编造知识点、题解、相似题。

## 题目介绍和注释

- Java 文件里的题目介绍必须原样粘贴进 C++，一个字都不要改。
- 题目介绍通常包括：
  - 中文题名
  - 题面描述
  - 测试链接
  - 提交说明
- 普通算法解释注释不要求全部保留。
- 翻译代码时可以删除 Java 风格的冗余实现注释，但不要改动题目介绍文本。
- 如果题目介绍中已经写了 `测试链接 : xxx`，C++ 代码里也保留原样。

## C++ 翻译风格

- 非 LeetCode 题尽量翻译成 ACM 风格：
  - 使用 `main`
  - 使用 `ios::sync_with_stdio(false);`
  - 使用 `cin.tie(nullptr);`
  - 按原题输入输出格式读写
- LeetCode 题使用 LeetCode 风格：
  - 保留 `class Solution`
  - 方法签名按 LeetCode C++ 常见写法
  - 不强行加 `main`
- 不要把 Java 的类结构硬翻成 C++ 类。
- 能写成全局数组、全局函数、局部变量的，就按竞赛风格自然组织。
- Java 的 `ArrayList`、`HashMap`、`PriorityQueue` 等容器要翻译成对应的 C++ STL 容器。
- 根据数据范围选择合适类型，可能溢出的累加和用 `long long`。
- 保证代码整体可读，变量命名可沿用原 Java 代码，也可以适度 C++ 化。

## 特别注意

- 不要修改 Java 源文件。
- 不要修改已有母题文件。
- 不要填 `涉及知识点`。
- 不要为了“像 Java”而保留别扭的类、静态方法、包装结构。
- 不要为了“像 C++”而改写题目介绍和题面注释。
- 对于一个课时目录批量处理时，只处理该目录下明确需要生成的新母题。
