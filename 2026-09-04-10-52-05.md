![[Pasted image 20260904105209.png|680]]

kmp的核心就是看前后缀,蓝色为已经配对上的前后缀
如图当前是2

就是看红色框里的ab 能不能接上c 蓝色 黑框里的ab 能不能接上c
如果两个 "c"一样,那就是能接上,前后缀变成3

如果黑框里是 abb ,红框里是 abc ,那么就要使用"回退" j = nxt[j] 
意为将前后缀回退到 以 ab为字符串,看看下一个待选的前后缀
例如 abaxxaba (xx为任意字符),
这里的 前后缀为3
如果是 abacxabab
那么它就要像上面说的那样回退到
abaxxaba 的前后缀小于3的前后缀.同等于aba的前后缀,也就是1
然后再使用a作为前后缀 匹配后续的字符
