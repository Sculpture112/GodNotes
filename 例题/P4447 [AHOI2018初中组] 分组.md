# []

> **原题链接:** ()

**涉及知识点:** [[贪心算法]], [[小根堆]]，[[补题]],[[映射]],[[排序预处理]]

**核心套路:** 

## 破题切入点 (思维闪念)
[]



```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<long long> a(n);
    for (auto &x : a) cin >> x;
    sort(a.begin(), a.end());

    using MinHeap = priority_queue<int, vector<int>, greater<int>>;
    unordered_map<long long, MinHeap> groups;

    for (long long x : a) {
        int len = 1;

		auto it = groups.find(x - 1);
		if (it != groups.end() && !it->second.empty()) {
			len = it->second.top() + 1;
			it->second.pop();
		}

        groups[x].push(len);
    }

    int answer = n;
    for (auto &[_, heap] : groups) {
        if (!heap.empty()) {
            answer = min(answer, heap.top());
        }
    }

    cout << answer << '\n';
    return 0;
}
```

---


