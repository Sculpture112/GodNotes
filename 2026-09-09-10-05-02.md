这行执行时，前面的条件已经保证：

```
i % p != 0
```

也就是说，`p` 是一个没有在 `i` 中出现过的新质因子。

莫比乌斯函数规定：如果一个数由 \(k\) 个不同质因子相乘得到，那么

$$ \mu(n)=(-1)^k $$

现在给 `i` 乘上一个新的质因子 `p`，质因子数量从 \(k\) 变成 \(k+1\)：

$$ \mu(i\cdot p)=(-1)^{k+1}=-(-1)^k=-\mu(i) $$

所以写成：

```
mu[i * p] = -mu[i];
```