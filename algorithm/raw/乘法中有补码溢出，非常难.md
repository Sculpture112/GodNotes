乘法中有补码溢出，非常难
```

```cpp

# include <climits>

# include <iostream>

class Solution {
public:
    // 加法：无进位相加 (^) 与 进位信息 (& << 1) 循环迭代
    int add(int a, int b) {
        while (b != 0) {
            int sum = a ^ b;                       // 无进位相加
            b = (unsigned int)(a & b) << 1;        // 计算进位并左移
            a = sum;
        }
        return a;
    }

    // 取负：取反加 1 (~n + 1)
    int neg(int n) { 
        return add(~n, 1); 
    }

    // 减法：a - b 等于 a + (-b)
    int minus(int a, int b) { 
        return add(a, neg(b)); 
    }

    // 乘法：模拟手工竖式乘法，b 的每一位 1 去乘并累加 a 的位移结果
    int multiply(int a, int b) {
        int ans = 0;
        unsigned int ub = (unsigned int)b;
```

```cpp
这里一定要把b转为无符号，这样可以处理负数，也可以处理a和b同时为负数
```

```cpp
while (ub != 0) {
            if (ub & 1) {
                ans = add(ans, a);                 // 只要 b 最右侧是 1，就累加当前的 a
            }
            a <<= 1; 
            ub >>= 1;
        }
        return ans;
    }

    // 基础除法：类似长除法，从高位向低位试除
    int div(int a, int b) {
        int x = (a < 0) ? neg(a) : a;
        int y = (b < 0) ? neg(b) : b;
        int ans = 0;

        for (int i = 30; i >= 0; i = minus(i, 1)) {
            if ((x >> i) >= y) {
                ans |= (1 << i);
                x = minus(x, y << i);
            }
        }
        return (a < 0 ^ b < 0) ? neg(ans) : ans;
    }

    // 完整除法：处理 INT_MIN 溢出和边界
    int divide(int a, int b) {
        if (a == INT_MIN && b == INT_MIN) return 1;
        if (b == INT_MIN) return 0;

        if (a == INT_MIN) {
            if (b == neg(1)) return INT_MAX;       // 溢出特例：-2147483648 / -1

            int offset = (b > 0) ? b : neg(b);
```

```cpp
遇到最小值，不能取绝对值，会超出数值范围。但是可以先将它减小（加上一个整数，也就是加b）
            // 补偿法：先加一个 |b| 缩小范围再除
            int ans = div(add(a, offset), b); 
            return add(ans, (b > 0) ? neg(1) : 1);
```

```cpp
再把它加回来
        }

        return div(a, b);
    }
};