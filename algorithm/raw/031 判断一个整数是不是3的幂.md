```cpp
// 031 判断一个整数是不是3的幂
class Solution {
public:
    // 核心逻辑：利用 3 是质数，且 3^19 是 int 范围内的极限 3 的幂
    bool isPowerOfThree(int n) {
        // 如果 n 是 3 的幂，则它只能包含质因子 3，因此必然能被 1162261467 整除
        return n > 0 && 1162261467 % n == 0;
    }
};
```