```cpp
// 030 只出现一次的数字
#include <vector>
using namespace std;

class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int eor = 0;
        // 核心逻辑：利用异或运算的消消乐特性，偶数次的数全抵消，剩下的就是奇数次的
        for (int num : nums) eor ^= num; 
        return eor;
    }
};
```