```cpp
// 034 判断链表是否是回文结构
#include <iostream>

struct ListNode {
    int val;
    ListNode *next;
};

class Solution {
public:
    bool isPalindrome(ListNode* head) {
        if (!head || !head->next) return true;
```

```cpp
1 -> 2 -> 3 <- 2 <- 1 核心

        // 1. 快慢指针找中点
        ListNode *slow = head;
        ListNode *fast = head;
        while (fast->next && fast->next->next) {
            slow = slow->next;
            fast = fast->next->next;
        }

        // 2. 反转后半部分链表
        ListNode *pre = slow;
        ListNode *cur = slow->next;
        ListNode *nxt = nullptr;
        pre->next = nullptr; // 断开前半部分

        while (cur) {
            nxt = cur->next;
            cur->next = pre;
            pre = cur;
            cur = nxt;
        }

        // 3. 双端对比判断回文
        ListNode *l = head;
        ListNode *r = pre; // pre 现在是原链表的尾节点，反转后的头节点
        bool ans = true;

        while (l && r) {
            if (l->val != r->val) {
                ans = false;
                break;
            }
            l = l->next;
            r = r->next;
        }

        // 4. 准备还原链表结构 (保持原样是良好的编程习惯)
        cur = pre->next;
        pre->next = nullptr;

        while (cur) {
            nxt = cur->next;
            cur->next = pre;
            pre = cur;
            cur = nxt;
        }

        return ans;
    }
};
```