// 037 收集累加和等于aim的所有路径
// 1. 二叉树节点结构定义 (TreeNode Struct)
// 2. 路径总和主函数 (pathSum)
// 3. 深度优先搜索与回溯逻辑 (f)
// 4. 路径拷贝辅助函数 (copy)

# include <vector>

using namespace std;

// 不提交这个类
struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};

class Code03_PathSumII {
public:
    // 提交如下的方法
    vector<vector<int>> pathSum(TreeNode* root, int aim) {
        vector<vector<int>> ans;
        if (root != nullptr) {
            vector<int> path;
            f(root, aim, 0, path, ans);
        }
        return ans;
    }

    // 递归函数 f
    // cur: 当前节点
    // aim: 目标累加和
    // sum: 到达当前节点上方的累加和
    // path: 当前路径轨迹
    // ans: 收集结果的容器
    void f(TreeNode* cur, int aim, int sum, vector<int>& path, vector<vector<int>>& ans) {
        if (cur->left == nullptr && cur->right == nullptr) {
            // 叶节点
            if (cur->val + sum == aim) {
                path.push_back(cur->val);
                copy(path, ans);
                // 回溯：移除当前添加的叶子节点值
                path.pop_back();
            }
        } else {
            // 不是叶节点
            path.push_back(cur->val);
            if (cur->left != nullptr) {
                f(cur->left, aim, sum + cur->val, path, ans);
            }
            if (cur->right != nullptr) {
                f(cur->right, n-aim, sum + cur->val, path, ans);
            }
            // 回溯：在返回父节点之前，清理当前路径
            path.pop_back();
        }
    }

    // 路径拷贝逻辑
    // 将当前满足条件的路径深度拷贝并存入答案
    void copy(const vector<int>& path, vector<vector<int>>& ans) {
        vector<int> cp;
        for (int num : path) {
            cp.push_back(num);
        }
        ans.push_back(cp);
        // 注意：在 C++ 中，ans.push_back(path) 本身就会触发拷贝构造
        // 这里为了完全对应 Java 原代码的手动拷贝逻辑进行编写
    }
};