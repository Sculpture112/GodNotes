// 035 插入、删除和获取随机元素 O(1) 允许重复

# include <unordered_map>

# include <unordered_set>

# include <vector>

# include <cstdlib>

using namespace std;
一个map一个arr
class RandomizedCollection {
    unordered_map<int, unordered_set<int>> map; // key: 值, value: 该值出现的所有下标集合
    vector<int> arr; // 存储实际数据
public:
    RandomizedCollection() {}

    // 插入：记录下标并加入数组。若该值之前不存在则返回 true
    bool insert(int val) {
        bool res = map[val].empty();
        map[val].insert(arr.size()); arr.push_back(val);
        return res;
    }

    // 删除：取任意一个下标，用数组末尾元素覆盖之。
    bool remove(int val) {
        if (map[val].empty()) return false;
        int idx = *map[val].begin(), lastVal = arr.back(); // 取出待删位和末尾值
        if (val == lastVal) { // 特殊处理：如果要删的就是末尾的值
            map[val].erase(arr.size() - 1);
        } else {
```

```cpp
先讲lastval的map中的地址改，然后再把arr中val（要删除）的数据用lastval覆盖。接着抹除lastval原本的map位置。再将val的map地址删除。
            map[lastVal].insert(idx); // 1. 末尾值的新家
            arr[idx] = lastVal;      // 2. 物理覆盖
            map[lastVal].erase(arr.size() - 1); // 3. 抹除末尾旧址
            map[val].erase(idx);     // 4. 抹除待删位
        }
```

```cpp
最后把lastval的原本arr中的位置减掉
        arr.pop_back(); // 5. 缩容
        return true;
    }

    // 随机获取：数组下标 O(1) 等概率
    int getRandom() { return arr[rand() % arr.size()]; }
};