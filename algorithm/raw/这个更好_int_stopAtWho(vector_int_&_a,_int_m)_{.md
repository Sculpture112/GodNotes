```cpp
这个更好          int stopAtWho(vector<int>& a, int m) {
    int n = a.size();
    int idx = 0;
    while (m--) {
        // 竞赛级核心公式：处理大步数 + 逆时针越界
        idx = (idx - a[idx] % n + n) % n;
    }
    return idx + 1;
}
```

        有 nn 个小朋友排成一圈，按照顺时针顺序依次被编号为 1∼n1∼n，每个小朋友衣服上都有一个数字，第 ii 个小朋友的数字是 aiai​。

旺仔哥哥想要选出一个小朋友，于是他先站在 11 号小朋友旁边，然后以如下方式移动 mm 次：
逆时针走过「当前小朋友衣服上的数字」数量个小朋友。旺仔哥哥想知道，他最后会站在哪位小朋友旁边。

你需要实现一个函数，包含以下两个参数：

∙ ∙一个序列 aa，第 ii 个小朋友的数字是 aiai​。

∙ ∙一个正整数 mm，表示旺仔哥哥的移动次数。
你需要计算出旺仔哥哥最后会站在哪位小朋友旁边，并将这个结果作为该函数的返回值。

示例1

输入：
```cpp
[2,1,4,5,2,3],3
```
复制

返回值：
```cpp
5
```
复制

说明：
```cpp
初始时，旺仔哥哥站在 11 号小朋友旁边。第 1 次移动前，11 号小朋友衣服上的数字 a1=2a1​=2，因此旺仔哥哥需要逆时针走过 22 个小朋友。旺仔哥哥走到 55 号小朋友旁边。第 2 次移动前，55 号小朋友衣服上的数字 a5=2a5​=2，因此旺仔哥哥需要逆时针走过 22 个小朋友。旺仔哥哥走到 33 号小朋友旁边。第 3 次移动前，33 号小朋友衣服上的数字 a3=4a3​=4，因此旺仔哥哥需要逆时针走过 44 个小朋友。旺仔哥哥走到 55 号小朋友旁边。最终旺仔哥哥站在 55 号小朋友旁边。
```

```cpp
class Solution {
public:
    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     *
     * 计算出旺仔哥哥最后会站在哪位小朋友旁边
     * @param a int整型vector 第 i 个小朋友的数字是 a_i
     * @param m int整型 表示旺仔哥哥的移动次数
     * @return int整型
     */
    int stopAtWho(vector<int>& a, int m) {
        // write code here
        int idx = 0;
        for(int i = 0;i<m;i++){
            int k = a[idx];
            while(idx-k<0){
                idx+=a.size();
            }
            idx=(idx-k+a.size())%a.size();
        }
        idx++;
        return idx;
    }
};
```

```cpp

```

```cpp

```