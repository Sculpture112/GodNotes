# []

> **原题链接:** (https://leetcode.cn/problems/binary-tree-zigzag-level-order-traversal/)

**涉及知识点:** [[]], [[]]，[[]],[[]],[[]]

**核心套路:** 

## 破题切入点(思维闪念)
[]

```cpp
#include <bits/stdc++.h>

using namespace std;

// 二叉树的锯齿形层序遍历
// 测试链接 : https://leetcode.cn/problems/binary-tree-zigzag-level-order-traversal/
class Solution {
public:
    // 提交以下的方法
    // 用每次处理一层的优化bfs就非常容易实现
    vector<vector<int>> zigzagLevelOrder(TreeNode* root) {
        vector<vector<int>> ans;
        if (root != nullptr) {
            queue<TreeNode*> q;
            q.push(root);
            // false 代表从左往右
            // true 代表从右往左
            bool reverse = false;
            while (!q.empty()) {
                int size = q.size();
                vector<int> list(size);
                for (int i = 0; i < size; i++) {
                    TreeNode* cur = q.front();
                    q.pop();
                    list[reverse ? size - 1 - i : i] = cur->val;
                    if (cur->left != nullptr) q.push(cur->left);
                    if (cur->right != nullptr) q.push(cur->right);
                }
                ans.push_back(list);
                reverse = !reverse;
            }
        }
        return ans;
    }
};
```

---

