# []

> **原题链接:** (https://leetcode.cn/problems/unique-substrings-in-wraparound-string/)

**涉及知识点:** [[一维DP]], [[字符串处理]]，[[需重刷]],[[]],[[]]

**难度 [[超难]]


```cpp
// 066 Code07 环绕字符串中唯一的子字符串

// 定义字符串 base 为一个 "abcdefghijklmnopqrstuvwxyz" 无限环绕的字符串
// 所以 base 看起来是这样的：
// "..zabcdefghijklmnopqrstuvwxyzabcdefghijklmnopqrstuvwxyzabcd.."
// 给你一个字符串 s ，请你统计并返回 s 中有多少 不同非空子串 也在 base 中出现
// 测试链接 : https://leetcode.cn/problems/unique-substrings-in-wraparound-string/
	
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

using namespace std;

class Solution {
public:
    // 时间复杂度 O(n)，n 是字符串 s 的长度，字符串 base 长度为正无穷
    int findSubstringInWraproundString(string s) {
        int n = s.length();
        if (n == 0) {
            return 0;
        }
        
        // dp[i] : s 中必须以对应字符（'a'对应0，'z'对应25）结尾的子串，
        // 最大延伸长度是多少，延伸一定要跟据 base 串的规则
        vector<int> dp(26, 0);
        
        // s : c d e....
        //     2 3 4
        dp[s[0] - 'a'] = 1;
        
        for (int i = 1, len = 1; i < n; i++) {
            int cur = s[i] - 'a';
            int pre = s[i - 1] - 'a';
            
            // pre cur
            // (前一个字符是'z' && 当前字符是'a') || 前一个字符比当前字符的 ascii 码少 1
            if ((pre == 25 && cur == 0) || pre + 1 == cur) {
                len++;
            } else {
                len = 1;
            }
            dp[cur] = max(dp[cur], len);
        }
        
        // 使用 C++ 标准库中的 accumulate 快速求和
        // 等价于遍历 dp 数组并累加
        return accumulate(dp.begin(), dp.end(), 0);
    }
};
```

---


**核心套路:** 
- **等价转换（最妙的一步）**：将“求唯一非空子串的数量”转化为“求**以每个字符结尾的最长连续合法子串长度**”。
    
    - _原理解释_：假如一个合法子串以 `'d'` 结尾，最大连续长度为 4（即 `"abcd"`），那么它必然包含了以 `'d'` 结尾的长度为 1, 2, 3 的子串（即 `"d"`, `"cd"`, `"bcd"`）。我们只需要记录以 `'d'` 结尾的最大长度 4，就能代表这 4 个不同的子串，天然完成了**去重**。
        
- **环绕连续性判定**：利用 ASCII 码的差值，当前字符与前一个字符满足 `pre + 1 == cur` 或 `pre == 'z' && cur == 'a'`。
    
- **分桶思想**：开辟一个大小为 26 的数组 `dp`，把巨大的子串集合按“最后一个字符”分进 26 个桶里，每个桶只保留最大值。
**前置母题/相似题目:** [[]]
**时空限制瓶颈:** []

---


## 破题切入点 (思维闪念)
[]
