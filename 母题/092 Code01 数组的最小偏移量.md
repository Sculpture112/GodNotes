# []

> **原题链接:** (https://leetcode.cn/problems/minimize-deviation-in-array/)

**涉及知识点:** [[]], [[]]，[[补题]],[[]],[[]]

**核心套路:** 

## 破题切入点 (思维闪念)
[]



```cpp
#include <bits/stdc++.h>
using namespace std;
// 数组的最小偏移量
// 测试链接 : https://leetcode.cn/problems/minimize-deviation-in-array/
class Solution {
public:
    int minimumDeviation(vector<int>& nums) {
        multiset<int> set;
        for (int num : nums) {
            set.insert(num % 2 == 0 ? num : num * 2);
        }
        int ans = *set.rbegin() - *set.begin();
        while (ans > 0 && *set.rbegin() % 2 == 0) {
            int maxValue = *set.rbegin();
            set.erase(prev(set.end()));
            set.insert(maxValue / 2);
            ans = min(ans, *set.rbegin() - *set.begin());
        }
        return ans;
    }
};
```

---
