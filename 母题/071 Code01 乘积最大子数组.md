# []

> **原题链接:** (https://leetcode.cn/problems/maximum-product-subarray/)

**涉及知识点:** [[已AC]], [[DP]]，[[需重刷]],[[]],[[]]

**核心套路:** 


```cpp
// 071 Code01 乘积最大子数组

// 给你一个整数数组 nums
// 请你找出数组中乘积最大的非空连续子数组
// 并返回该子数组所对应的乘积
// 测试链接 : https://leetcode.cn/problems/maximum-product-subarray/

#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    int maxProduct(const vector<int>& nums) {
        if (nums.empty()) return 0;

        // 这节课讲完之后，测试数据又增加了
        // 用 int 或 long long 类型的变量可能会让中间结果溢出
        // 所以改成用 double 类型的变量
        // 思路是不变的
        double ans = nums[0];
        double cur_min = nums[0];
        double cur_max = nums[0];

        for (int i = 1; i < nums.size(); i++) {
            double num = nums[i];
            
            // 使用 C++11 初始化列表一次性求出三个数的最值
            double next_min = min({num, cur_min * num, cur_max * num});
            double next_max = max({num, cur_min * num, cur_max * num});
            
            cur_min = next_min;
            cur_max = next_max;
            ans = max(ans, cur_max);
        }
        
        return (int)ans;
    }
};
```

---



**前置母题/相似题目:** [[]]
**时空限制瓶颈:** []

---


## 破题切入点 (思维闪念)
[]
