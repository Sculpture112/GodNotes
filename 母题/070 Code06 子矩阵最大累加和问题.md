# []

> **原题链接:** (https://leetcode.cn/problems/max-submatrix-lcci/
)

**涉及知识点:** [[已AC]], [[Kadane]]，[[需重刷]],[[]],[[]]

**核心套路:** 
1. **二维问题一维化**：面对二维矩阵寻找最大累加和区域时，直接枚举四个边界的时间复杂度是 $O(N^4)$。核心套路是通过**固定上边界、滑动下边界**，将多行数据按列累加，压缩成一个一维数组。
2. **复用一维最大子数组和（Kadane算法）**：对压缩后的这个一维数组，直接套用求解“一维数组最大连续子数组和”的动态规划算法。 
3. **坐标映射**：在做一维 Kadane 算法求最大和的同时，维护一维的左右边界指针（起始列和终止列）。配合外层循环固定的上下边界（起始行和终止行），即可精准定位出最大子矩阵的四个坐标。


```cpp
// 070 Code06 子矩阵最大累加和问题

// 给定一个二维数组grid，找到其中子矩阵的最大累加和
// 返回拥有最大累加和的子矩阵左上角和右下角坐标
// 如果有多个子矩阵都有最大累加和，返回哪一个都可以
// 测试链接 : https://leetcode.cn/problems/max-submatrix-lcci/

#include <iostream>
#include <vector>
#include <algorithm>
#include <climits>

using namespace std;

class Solution {
public:
    // 如果行和列的规模都是n，时间复杂度O(n^3)，最优解了
    vector<int> getMaxMatrix(const vector<vector<int>>& grid) {
        int n = grid.size();
        if (n == 0) {
            return {};
        }
        int m = grid[0].size();
        
        int max_sum = INT_MIN;
        int a = 0, b = 0, c = 0, d = 0;
        
        // 用于压缩多行数据的一维数组
        vector<int> nums(m, 0);
        
        for (int up = 0; up < n; up++) {
            // 每次更换上边界，都需要清空压缩数组
            fill(nums.begin(), nums.end(), 0);
            
            for (int down = up; down < n; down++) {
                // 如下代码就是题目1的附加问题 :
                // 子数组中找到拥有最大累加和的子数组
                for (int l = 0, r = 0, pre = INT_MIN; r < m; r++) {
                    // 将当前行的数据累加到压缩数组中
                    nums[r] += grid[down][r];
                    
                    if (pre >= 0) {
                        pre += nums[r];
                    } else {
                        pre = nums[r];
                        l = r; // 换开头
                    }
                    
                    if (pre > max_sum) {
                        max_sum = pre;
                        a = up;    // 左上角行
                        b = l;     // 左上角列
                        c = down;  // 右下角行
                        d = r;     // 右下角列
                    }
                }
            }
        }
        
        // C++11 支持直接返回初始化列表来构造 vector
        return {a, b, c, d};
    }
};
```

---



**前置母题/相似题目:** [[]]
**时空限制瓶颈:** []

---


## 破题切入点 (思维闪念)
[]
