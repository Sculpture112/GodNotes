# []

> **原题链接:** (https://leetcode.cn/problems/house-robber/)

**涉及知识点:** [[已AC]], [[DP]]，[[需重刷]],[[]],[[]]

**核心套路:** 
1. 状态定义：`dp[i]` 表示在数组 `nums[0...i]` 范围内，不选择相邻元素所能得到的最大累加和。
2. 状态转移方程：对于第 `i` 个位置，有两种主要选择： 
- 不选 `nums[i]`：最大和继承自前一个位置，即 `dp[i - 1]`
- 选 `nums[i]`：最大和为前两个位置的最大和加上当前值，即 `dp[i - 2] + nums[i]`
- (注：代码中加入了 `nums[i]` 单独成军的情况，是为了兼容数组中可能存在负数的通用情况。如果已知都是非负数，`max(dp[i-1], dp[i-2] + nums[i])` 即可)。 
1. 空间压缩（滚动数组）：由于 `dp[i]` 的状态只依赖于 `dp[i-1]` 和 `dp[i-2]`，可以用两个变量 `pre` 和 `prepre` 交替滚动更新，将空间复杂度从 $O(N)$ 降至 $O(1)$。


```cpp
// 070 Code02 数组中不能选相邻元素的最大累加和

// 给定一个数组，可以随意选择数字
// 但是不能选择相邻的数字，返回能得到的最大累加和
// 测试链接 : https://leetcode.cn/problems/house-robber/

#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    int rob(vector<int>& nums) {
        // 两种方法任选其一均可
        // return rob1(nums);
        return rob2(nums);
    }

    // 动态规划
    int rob1(const vector<int>& nums) {
        int n = nums.size();
        if (n == 0) {
            return 0;
        }
        if (n == 1) {
            return nums[0];
        }
        if (n == 2) {
            return max(nums[0], nums[1]);
        }
        
        // dp[i] : nums[0...i]范围上可以随意选择数字，但是不能选相邻数，能得到的最大累加和
        vector<int> dp(n);
        dp[0] = nums[0];
        dp[1] = max(nums[0], nums[1]);
        
        for (int i = 2; i < n; i++) {
            // C++11 支持使用初始化列表求多个值的最大值
            dp[i] = max({dp[i - 1], nums[i], dp[i - 2] + nums[i]});
        }
        return dp[n - 1];
    }

    // 空间压缩
    int rob2(const vector<int>& nums) {
        int n = nums.size();
        if (n == 0) {
            return 0;
        }
        if (n == 1) {
            return nums[0];
        }
        if (n == 2) {
            return max(nums[0], nums[1]);
        }
        
        int prepre = nums[0];
        int pre = max(nums[0], nums[1]);
        
        for (int i = 2; i < n; i++) {
            int cur = max({pre, nums[i], prepre + nums[i]});
            prepre = pre;
            pre = cur;
        }
        return pre;
    }
};
```

---



**前置母题/相似题目:** [[]]
**时空限制瓶颈:** []

---


## 破题切入点 (思维闪念)
[]
