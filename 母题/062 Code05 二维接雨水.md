# []

> **原题链接:** (https://leetcode.cn/problems/trapping-rain-water-ii/)

**涉及知识点:** [[已AC]], [[]]，[[需重刷]]

**难度 [[]]


```cpp
// 062 Code05 二维接雨水

// 给你一个 m * n 的矩阵，其中的值均为非负整数，代表二维高度图每个单元的高度
// 请计算图中形状最多能接多少体积的雨水。
// 测试链接 : https://leetcode.cn/problems/trapping-rain-water-ii/

#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>

using namespace std;

// 题目规定 m, n <= 200
const int MAXN = 205;
const int MAXM = 205;

bool visited[MAXN][MAXM];

// 0:上，1:右，2:下，3:左 的坐标偏移
int mv[5] = { -1, 0, 1, 0, -1 };

// 定义单元格结构体
struct Cell {
    int r;
    int c;
    int w; // 水线 (water line)

    // C++ 的 priority_queue 默认是大根堆
    // 为了实现小根堆，我们需要反转比较逻辑 (w > other.w)
    bool operator<(const Cell& other) const {
        return w > other.w;
    }
};

class Solution {
public:
    int trapRainWater(vector<vector<int>>& height) {
        int n = height.size();
        int m = height[0].size();
        
        // LeetCode 会重用对象，必须重置全局状态
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                visited[i][j] = false;
            }
        }
        
        priority_queue<Cell> pq;
        
        // 将所有四周边界加入小根堆，并标记为已访问
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                if (i == 0 || i == n - 1 || j == 0 || j == m - 1) {
                    pq.push({i, j, height[i][j]});
                    visited[i][j] = true;
                }
            }
        }
        
        int ans = 0;
        
        // 从最外围最矮的“桶壁”开始往内部灌水
        while (!pq.empty()) {
            Cell record = pq.top();
            pq.pop();
            
            int r = record.r;
            int c = record.c;
            int w = record.w;
            
            // 累加当前格子能接的水量
            // (初始边界格子的 w 等于其 height，差值为 0，不会多加)
            ans += w - height[r][c];
            
            // 尝试向 4 个方向扩展
            for (int i = 0; i < 4; i++) {
                int nr = r + mv[i];
                int nc = c + mv[i + 1];
                
                if (nr >= 0 && nr < n && nc >= 0 && nc < m && !visited[nr][nc]) {
                    // 相邻格子的水线取决于当前水线和它自身高度的最大值（木桶效应）
                    pq.push({nr, nc, max(height[nr][nc], w)});
                    visited[nr][nc] = true;
                }
            }
        }
        
        return ans;
    }
};
```

---


**核心套路:** [[]]
**前置母题/相似题目:** [[]]
**时空限制瓶颈:** []

---


## 破题切入点 (思维闪念)
[]
