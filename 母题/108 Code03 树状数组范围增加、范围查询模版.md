# [108 Code03 树状数组范围增加、范围查询模版]

> **原题链接:** (https://www.luogu.com.cn/problem/P3372)

**涉及知识点:** [[]], [[]]，[[补题]],[[]],[[]]

**核心套路:** 

## 破题切入点 (思维闪念)
[]



```cpp
#include <bits/stdc++.h>
using namespace std;

// 树状数组范围增加、范围查询模版
// 测试链接 : https://www.luogu.com.cn/problem/P3372
// 请同学们务必参考如下代码中关于输入、输出的处理
// 这是输入输出处理效率很高的写法
// 提交以下的code，提交时请把类名改成"Main"，可以直接通过

const int MAXN = 100001;

long long info1[MAXN];
long long info2[MAXN];
int n, m;

int lowbit(int i) {
    return i & -i;
}

void add(long long tree[], int i, long long v) {
    while (i <= n) {
        tree[i] += v;
        i += lowbit(i);
    }
}

long long sum(long long tree[], int i) {
    long long ans = 0;
    while (i > 0) {
        ans += tree[i];
        i -= lowbit(i);
    }
    return ans;
}

void add(int l, int r, long long v) {
    add(info1, l, v);
    add(info1, r + 1, -v);
    add(info2, l, (long long)(l - 1) * v);
    add(info2, r + 1, -(long long)r * v);
}

long long range(int l, int r) {
    return sum(info1, r) * r - sum(info2, r) - sum(info1, l - 1) * (l - 1) + sum(info2, l - 1);
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        long long cur;
        cin >> cur;
        add(i, i, cur);
    }
    for (int i = 1, op, l, r; i <= m; i++) {
        cin >> op;
        if (op == 1) {
            long long v;
            cin >> l >> r >> v;
            add(l, r, v);
        } else {
            cin >> l >> r;
            cout << range(l, r) << '\n';
        }
    }
    return 0;
}
```

---

