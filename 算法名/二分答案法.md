```cpp
// 模板一：答案在 l
while (l < r)
{
    mid = l + (r - l) / 2;
    if (check(mid)) r = mid;
    else l = mid + 1;
}
// 模板二：答案在 r
while (r - l > 1)
{
    mid = l + (r - l) / 2;
    if (check(mid)) r = mid;
    else l = mid;
}
```