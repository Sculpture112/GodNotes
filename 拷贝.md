```c++
// 替换你原代码中最后那个复杂的 for (int i = 0; i < n;) 块
int curr_pos = 0;

if (l2 > 0) {
    std::memcpy(arr + curr_pos, arr2, l2 * sizeof(int));
    curr_pos += l2;
}
if (x > 0) {
    std::memcpy(arr + curr_pos, arrx, x * sizeof(int));
    curr_pos += x;
}
if (l3 > 0) {
    std::memcpy(arr + curr_pos, arr3, l3 * sizeof(int));
    curr_pos += l3;
}
if (l6 > 0) {
    std::memcpy(arr + curr_pos, arr6, l6 * sizeof(int));
    curr_pos += l6;
}
`#include <bits/stdc++.h>

using namespace std;

// #define int long long

const int maxn = 2 * 1e5 + 5;

int arr[maxn];

int arr2[maxn];

int arr3[maxn];

int arr6[maxn];

int arrx[maxn];

void solve()

{

    int t;

    cin >> t;

    while (t--)

    {

        int n;

        cin >> n;

        for (int i = 0; i < n; i++)

        {

            cin >> arr[i];

        }

        int l2 = 0, l3 = 0, l6 = 0, x = 0;

  

        for (int i = 0; i < n; i++)

        {

            if (arr[i] % 6 == 0)

            {

                arr6[l6++] = arr[i];

            }

            else if (arr[i] % 3 == 0)

            {

                arr3[l3++] = arr[i];

            }

            else if (arr[i] % 2 == 0)

            {

                arr2[l2++] = arr[i];

            }

            else

            {

                arrx[x++] = arr[i];

            }

        }

        for (int i = 0; i < n;)

        {

            for (; i < l2; i++)

            {

                arr[i] = arr2[i];

            }

  

            for (int j = 0; j < x; i++, j++)

            {

                arr[i] = arrx[j];

            }

            for (int j = 0; j < l3; i++, j++)

            {

                arr[i] = arr3[j];

            }

            for (int j = 0; j < l6; i++, j++)

            {

                arr[i] = arr6[j];

             }

        }

  

        cout << arr[0];

        for (int i = 1; i < n; i++)

        {

            cout << " " << arr[i];

        }

        cout << "\n";

    }

}

signed main()

{

    ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);

    solve();

    return 0;

}`
```