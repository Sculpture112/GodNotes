1. **纯注释模式**：输出一份完整的 C++ 代码模版，但**删掉所有具体的函数实现和逻辑代码**。只保留必要的 `#include`、全局变量定义、常量定义和函数签名。(注意，这里的变量命名由我亲自书写，当然函数名不用)
    
2. **极致详尽的注释**：在代码的空位处，按照逻辑顺序（1, 2, 3...）填入极其详尽的注释。每一行注释都要描述“这一步在逻辑上要做什么”，而不是“写哪行代码”。

3. 不要带有中二描述风格！！