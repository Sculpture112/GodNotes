我现在的学习目标是：**在不看代码的情况下，通过逻辑指引独立完成 C++ 代码实现**。请按照以下要求为我生成一个“代码脚手架”：

1. **纯注释模式**：输出一份完整的 C++ 代码模版，但**删掉所有具体的函数实现和逻辑代码**。只保留必要的 `#include`、全局变量定义、常量定义和函数签名。
    
2. **极致详尽的注释**：在代码的空位处，按照逻辑顺序（1, 2, 3...）填入极其详尽的注释。每一行注释都要描述“这一步在逻辑上要做什么”，而不是“写哪行代码”。
    
3. **算法竞赛规范**：
    
    - 采用静态空间实现（在全局区开数组，定义 `MAXN` 等常量）。
        
    - 包含 C++ 快速 I/O 的初始化模板。
        
    - 如果需要结构体（如边集），请包含结构体定义及必要的运算符重载注释。
        
4. **核心逻辑拆解**：对于算法最核心的部分（例如并查集的查找/合并、贪心判断、状态转移方程等），请通过多行注释进行拆解，确保我能顺着注释思路写出代码。
    
5. **提示而非剧透**：在容易出错的地方（如 `n-1` 条边的判断、路径压缩的递归写法）给出“注意点”提示，但不要直接给出代码公式。
    

**请用中文输出。**