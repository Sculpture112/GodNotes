# 母题变式准则

本文档用于规范从母题生成“变题 / 补题 / 相似题”文件时的选题与写法。

## 必须使用 ys

- 生成母题变式时，必须先读取并使用 `$ys`：
  - `C:\Users\17487\.codex\skills\ys\SKILL.md`
- 如果 `$ys` 要求读取参考文件，比如 `references/variant-selection.md`，也必须先读完再选题。
- 输出风格按 `$ys`：先诊断母题类型，再提炼方法核心，再给变式梯度。

## 变式不是自造题

- 每一个变式都必须有真实题目链接。
- 变式必须写成 Markdown 链接。
- 如果找不到真实链接，就换掉这道变式，不要保留自造题。
- 不要只写题意、不放链接。
- 不要用“类似某题”“可自己构造”代替真实链接。

## 选题顺序

每组母题先按 `$ys` 的路线判断：

1. 母题诊断：这题属于什么类型。
2. 方法核心：这题真正训练的可迁移方法是什么。
3. 选题镜头：用哪个变式维度来选题。
4. 测试点：这类题最容易错在哪里。
5. 变题：按梯度放真实链接题。
6. 方法总结：提炼这一组题的识别法。

Key point: 先找方法核心，再找题。不能只因为题名或主题相似就放进去。

## 常用变式梯度

按 `$ys` 的变式梯度选题，优先使用这些类型：

- Same-shell variant：外壳几乎不变，只换条件、数值、对象或限制。
- Changed-question variant：条件类似，但问法改变。
- Changed-representation variant：同一方法换成另一种表示，比如数组、区间、图、字符串、状态机。
- Added-restriction variant：增加一个边界、数量上限、合法性限制或隐藏条件。
- Changed-entry variant：方法核心不变，但入口不同。
- Comprehensive variant：和附近方法组合，但不能跳太远。

## 写法要求

- 文件标题用清晰的主题名，例如 `082 083变题.md`。
- 每个方法族或母题用二级标题。
- 每组建议包含：
  - 对应母题
  - 母题诊断
  - 方法核心
  - 选题镜头
  - 测试点
  - 变题
  - 方法总结
  - 最后的 Reflection
- 可以使用 `Key point:` 标出真正要抓住的思维点。
- 变题条目下面要说明“变化在哪里”，让人看出为什么它属于这个母题方法族。

## 排除规则

- 不要选母题文件夹里已经有的同名题作为补题，除非明确说明它是 Example problem 且用户允许。
- 不要放没有真实链接的自造变式。
- 不要放方法核心完全不同的题。
- 不要为了凑数量放跨度太大的综合题。
- 不要只按平台或标签选题，要按方法核心选题。

## 检查清单

生成后必须检查：

- 每个变题条目都有 `https://` 链接。
- 每个变题都说明了变化维度。
- 没有残留无链接的自造题。
- 没有和已有母题文件明显重复的题。
- 整体能回答：这组题共同训练的到底是什么方法。
