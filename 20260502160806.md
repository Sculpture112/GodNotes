- 💡 **[看到“翻转”想“拼接”]**：翻转一段数组不会改变里面元素的总和，但会改变它们的**相对位置**。它能把原本被负数隔开的两个正数块“缝合”在一起。
    
- 💡 **[消除变量]**：题目瞬间从“找翻转区间”变成了“找两个独立的最优子数组”。
    
- 💡 **[为什么是 `start[i]` 而不是右侧全局最大？]**：因为左侧的 `maxEnd` 包含了 `0` 到 `i-1` 范围内的任意位置的最优解，为了保证左右两部分**绝对不重叠**，右侧必须严格限定从切分线 `i` 开始（`start[i]`），这样两者相加组合时才不会发生元素复用的逻辑重叠。