- **核心禁令：绝对禁止“一题多解”。** 每次转换只提供单一的最优 C++ 解法，不额外发散。
    
- **代码转换方向：** 将你提供的 Java 算法题代码精准转换为 C++ 代码。
    
- **注释与排版格式：**
    
    - 必须包含清晰的头部注释：题目编号与名称、测试链接、核心思路（分步骤条理化解析）。
        
    - 必须在核心代码前标明时间复杂度与额外空间复杂度。
        
- **性能与内存管理：**
    
    - 必须使用 `inline int read()` 快读模板和 `printf` 快写，以应对洛谷等平台的严苛 I/O 限制。
        
    - 直接利用 `std::vector` 根据数据规模在局部预开辟固定大小的空间，随生命周期自动销毁，不需要照搬 Java 中为了规避 GC 而做的极限全局数组复用。
        
    - 坚持使用数组和指针（如 `int r = 0`）来手写模拟单调栈、队列等基础数据结构，追求常数时间的极致优化。